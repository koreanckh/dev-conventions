---
name: <skill-이름>
description: <언제 이 문서를 열어야 하는가 — 상황 트리거로 쓴다. "…할 때" 형태>
---

<!--
이 파일은 새 전역 규칙(skill)의 골격이다. `global/skills/<이름>/SKILL.md`로 복사해 채운다.
- `description`은 목차가 아니라 **트리거**다. 에이전트가 이 한 줄만 보고 "지금 열어야 하나"를 판단한다.
- 원칙: (1) 자기완결형 — 원칙 + 값 + 주의가 이 한 파일 안에. (2) 실제 config 값은 산문 말고 `project/stacks/<stack>/`·`global/enforcement/` 실파일로.
  (3) 프로젝트마다 달라지는 값은 본문에 박지 말고 **파라미터 이름으로 참조**하고 맨 아래 "필요한 프로젝트 파라미터"에 모은다.
- 매 작업에 적용되고 잊으면 안 지켜지는 규칙은 skill이 아니라 `global/always-on.md`로, 어기면 사고인 규칙은 `global/enforcement/`로 올린다(→ 설계 §4 Q2).
- 채울 때 이 주석 블록은 지우고, 섹션 순서는 유지한다. 문체 레퍼런스: `global/skills/todo-workflow/SKILL.md`, `global/skills/coding-conventions/SKILL.md`.
-->

# <규칙명>

<!-- 한 문단: 이 규칙이 무엇을 다루는가 + 프로젝트마다 달라지는 값은 어떤 파라미터로 읽는가. -->

## 원칙

<!-- 이 규칙의 비타협 핵심. 불릿 몇 개로 짧게. 모델이 이미 아는 일반론은 쓰지 않는다 —
     채택 기준: "이 문장이 없으면 에이전트가 다르게 행동하는가?" -->
-

## 셋업 / 값

<!-- 실제 설정 값이 있으면 실파일로 두고 여기선 "무엇을/왜"만.
     복붙 가능한 셋업 명령이 있으면 코드블록으로. 없으면 이 섹션 삭제. -->

## 주의 / 알려진 불일치

<!-- 함정, 미해결 불일치, 버전 의존성 등. 없으면 이 섹션 삭제. -->

## 필요한 프로젝트 파라미터

<!-- 이 규칙이 이름으로 참조하는 파라미터만. 새 키를 만들면 `project/AGENTS.template.md`를 **같은 커밋에서** 고친다 — 키 이름이 전역과 프로젝트의 계약이다. -->

- `<키>` — <이 규칙이 그 값으로 무엇을 하는가>
