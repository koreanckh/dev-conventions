# <규칙명>

> 원본(SSOT): dev-conventions. 대상 repo로 복사할 땐 이 줄을 `> 출처: dev-conventions · 복사 YYYY-MM-DD`로 바꿔 남긴다(복사본이 낡았는지 판단용).

<!--
이 파일은 새 규칙 문서의 골격이다. `conventions/<이름>.md`로 복사해 채운다.
원칙: (1) 자기완결형 — 원칙+셋업+적용법이 이 한 파일 안에. (2) 값/설정은 산문 말고 `templates/<이름>/` 실파일로. (3) 반드시 맨 아래 "이 규칙 적용하기"로 끝낸다.
채울 때: 아래 <!-- --> 주석은 다 지우고, 섹션 순서는 유지한다. 문체는 기존 todo-workflow.md / coding-conventions.md를 레퍼런스로.
-->

<!-- 한 문단: 이 규칙이 무엇을 다루는가 + 프로젝트마다 바꿔 쓸 값(`<owner/repo>` 같은)은 무엇인가. -->

## 원칙

<!-- 이 규칙의 비타협 핵심. 불릿 몇 개로 짧게. -->
-

## 셋업 / 값

<!-- 실제 설정 값이 있으면 `templates/<이름>/`에 파일로 두고 여기선 "무엇을/왜"만.
     복붙 가능한 셋업 명령이 있으면 코드블록으로. 없으면 이 섹션 삭제. -->

## 주의 / 알려진 불일치

<!-- 함정, 미해결 불일치, 버전 의존성 등. 없으면 이 섹션 삭제. -->

## 이 규칙 적용하기

1. 이 문서를 대상 repo `docs/conventions/<이름>.md`로 복사하고 상단 출처 줄을 복사일로 채운다.
2. (값이 있으면) `templates/<이름>/` 파일을 대상 repo에 복사.
3. (셋업이 있으면) 셋업 명령을 1회 실행.
4. `AGENTS.md`의 `## 공통 규칙`에 포인터 한 줄 추가(전체 블록은 `templates/AGENTS.snippet.md`):
   ```markdown
   - <이럴 때 읽어라 힌트>: docs/conventions/<이름>.md
   ```
